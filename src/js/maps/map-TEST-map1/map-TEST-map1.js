// map-TEST-map1 — carte de test (la toute premiere, prototype).
// Seule carte a porter l'ecran de tele.

EnregistrerCarte({
  cle: 'map-TEST-map1',
  numero: 'TEST',
  nom: 'map1',
  // pas de carte suivante
  features: [TextesDeZone, Tele],
});
